services:
  oci-panel:
    build: .
    container_name: oci-panel
    ports:
      - "8080:8080"
    environment:
      # 不设置则首次启动自动生成随机密码(打印在日志里)
      - PANEL_PASSWORD=${PANEL_PASSWORD:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
